# Personal/working files — not published
personal-notes/
spec_design/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
pids/
*.pid
*.seed

# CRA build output
webapp/client/build/
webapp/client/.eslintcache

# Real DB credentials — keep the template (config.example.json) only
webapp/server/config.json

# Env files with real values
.env
.env.local
webapp/client/.env
webapp/client/.env.local
webapp/client/.env.development.local
webapp/client/.env.test.local
webapp/client/.env.production.local
webapp/server/.env
webapp/server/.env.local

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Test output
coverage/

# Tests — kept local, not published
__tests__/
*.test.js
*.test.jsx
webapp/client/src/setupTests.js

# Build / cache artifacts
dist/
.cache/
.parcel-cache/
.next/
.turbo/
.temp/
tmp/

# Local DB imports (too large for git, user-specific paths)
webapp/db/import/
*.csv

# Local Postgres data if anyone bind-mounts into the repo
webapp/db/pgdata/
webapp/db/data/
